apply plugin: 'java-library'

ext {
    version = "1.1.1"
    artifactId = "annotation"
    groupId = "com.source.annotation"
    packaging = "jar"
}
apply from: rootProject.uri('maven.gradle')
dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}